class User {
    constructor(id, name, img, team_domain) {
        this.id = id;
        this.name = name;
        this.img = img;
        this.team_domain = team_domain;
    }
}

module.exports = User;